import { defineSchema } from 'convex/server'

// Application tables are introduced in their implementation phases. Better
// Auth stores its own tables inside the mounted Convex component.
export default defineSchema({})
